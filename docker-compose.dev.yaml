services:
  nextjs-frontend-main:
    build:
      context: ./frontend/nextjs-frontend-main
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/nextjs-frontend-main:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev